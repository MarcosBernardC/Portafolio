//nav.js

// Función para cargar el archivo nav.html en el contenedor #nav-container
function cargarNav() {
    const navHTML = `
        `;
        // Insertar el código HTML directamente en el contenedor
        document.getElementById('nav-container').innerHTML = navHTML;
}
window.onload = cargarNav;  // Cargar el nav.html al cargar la página